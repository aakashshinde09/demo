version: 0.0
os: linux

files:
  - source: s3://perimattic-task-cloudfront-s3/demo.zip
    destination: /home/ec2-user/angular-frontend/demo.zip

hooks:

  BeforeInstall:
    - location: scripts/before_install.sh
      timeout: 300
      runas: root

  AfterInstall:
    - location: scripts/after_install.sh
      timeout: 300
      runas: root

  ApplicationStart:
    - location: scripts/application_start.sh
      timeout: 300
      runas: root